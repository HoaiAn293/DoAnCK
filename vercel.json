{
    "buildCommand": "npx expo export --platform web",
    "outputDirectory": "dist",
    "env": {
      "EXPO_PUBLIC_API_URL": "@api-url"
    },
    "rewrites": [
      { "source": "/(.*)", "destination": "/" }
    ]
  }